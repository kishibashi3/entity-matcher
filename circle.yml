dependencies:
  override:
    - ./gradlew -v
    - ./gradlew resolveDependencies
general:
  artifacts:
    - "build/reports"
machine:
  environment:
    _JAVA_OPTIONS: -Xmx500M
  java:
    version: oraclejdk8
  post:
    - sudo service mysql stop
    - sudo service postgresql stop
deployment:
  release:
    branch: master
